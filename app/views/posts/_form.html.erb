<%= form_for @post,:url => posts_path, :html => { :multipart => true } do |f| %>
  <% if @post.errors.any? %>
	</br>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>
      <ul>
      <% @post.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

	<div id="text-area">
		
		<div id="upload">  <%= f.file_field :photo %></div>
		<%= f.text_area :text, class: "inputs" %>
		<div class="actions">
			<%= f.submit "Post", :class => "button_post" %>
			<%= button_to_function "Cancel", "history.back()", :class => "button_post" %>
		</div>
	</div>
<% end %>
